# Dependencies
node_modules/
dashboard/node_modules/

# Build output (dev). The committed plugin bundle lives in pr-map-plugin/dist + dashboard-dist.
/dist/
dashboard/dist/

# Runtime working data (per-PR graphs, raw data, pending reviews)
.pr-map/

# Logs and OS noise
*.log
.DS_Store

# Editor
.vscode/
.idea/

# Autonomous run artifacts (ralph loop + /deliver reviewer). Ephemeral, not for the PR.
.ralph-task.md
plan.md
learnings.txt
review-notes.txt
review-findings.md
