pr-map — context and decisions (seed for autonomous execution)

WHAT WE ARE BUILDING
A Claude Code plugin/skill `/pr-map <pr-ref>` that turns a GitHub PR into an
interactive web mind map and lets the user review it (inline + general comments +
verdict) from the browser, posting to GitHub via the `gh` CLI.

KEY DECISIONS (locked during brainstorming)
- Surface: interactive web dashboard (browser), launched from the CLI. NOT terminal/ASCII.
- Engine: step-by-step pipeline. (1) changed files -> nodes. (2) static regex import
  scan -> edges + one-hop neighbors; no relation -> isolated node. (3) Claude Code agent
  enriches: writes the "why" per node/edge, may add semantic edges. Every edge tagged
  origin (static|llm) + confidence so LLM-inferred links are never confused with certain ones.
- Distribution: Claude Code plugin/skill. The "LLM step" is the agent at runtime — no
  Anthropic API key, no separate billing. Core (graph builder + dashboard) kept as
  separate Node modules so a standalone CLI is a cheap future add.
- Review actions: FULL review — inline comments, general comment, verdict
  (approve/request-changes/comment), and replies to existing threads. All via `gh`.
- Review UX: redacted and submitted from the dashboard. A small local Node backend
  relays to `gh`. GitHub's pending-then-submit model is respected (comments batch
  locally, then one submit with the verdict).
- Graph scope: PR files + one-hop neighbors (unchanged files that connect), neighbors
  styled distinctly (inPr:false). Includes incoming edges (who imports the changed
  files = blast radius) via `git grep`, and outgoing edges (what the changed files import).
- Static edges v1: lightweight regex import-rules, NOT full parsers. Start with JS/TS +
  Python. Adding a language = adding a rule.

ENVIRONMENT
- gh 2.88.1 authenticated as ValentinDutra, scope includes `repo` (ssh git protocol).
- node v25.2.1, python3 3.14.5, ripgrep (rg) available. `git grep` is the primary
  cross-repo reference finder (fast, tracked files only).
- Private repo `pr-map` under github.com/ValentinDutra.

REFERENCE (study, do NOT fork)
Understand-Anything (Egonex-AI, MIT). Its dashboard stack we mirror a subset of:
React 19 + Vite + @xyflow/react (React Flow) + dagre + Tailwind + prism-react-renderer.
We drop graphology/Louvain/elkjs/d3-force (those are for whole-codebase graphs).
Useful files to read for patterns:
  understand-anything-plugin/packages/dashboard/src/components/GraphView.tsx
  understand-anything-plugin/packages/dashboard/src/components/CodeViewer.tsx

USER CODING PREFERENCES (from CLAUDE.md)
- Result pattern for error handling; Retry pattern for transient failures.
- Dependency Injection everywhere feasible (inject the gh executor, inject gh-client).
- Self-documenting code, no abbreviations (comparisonPeriod, not cp).
- Tests are liability: minimum tests, maximum value. Test logic units (gh-client, edge
  detection, review endpoints) with injected fakes/fixtures. Do NOT test pure UI.
- No Claude attribution in commits or PRs.

EXECUTION NOTES (appended per task)
- Task 1: ESLint v9 flat config via `typescript-eslint` + `@eslint/js`; root config ignores
  `dashboard/**` so the dashboard (React) lints separately. `tsconfig.json` uses NodeNext +
  `include: ["src"]`; needs at least one .ts file or tsc errors "no inputs". `tseslint.configs.recommended`
  (not type-checked) avoids needing parserOptions.project. express pinned ^4 with @types/express ^4.
- Task 2: NodeNext requires explicit `.js` extensions on relative imports in .ts files
  (e.g. `from './result.js'`). Import vitest's `describe/it/expect` from 'vitest' (no globals
  config needed). `tsc --noEmit` also typechecks *.test.ts (they're under src/).
- Task 3: `gh api` resolves `{owner}`/`{repo}` placeholders from the current dir's git remote,
  so review/file/diff calls don't need owner/repo args (only the local checkout). PR metadata
  comes from `gh pr view --json ...,url`; owner/repo parsed from the url. Changed files +
  per-file patch + status come from `gh api repos/{owner}/{repo}/pulls/<n>/files --paginate`
  (GitHub status 'removed' maps to our 'deleted'). createGhClient takes retryOptions for DI
  (tests pass delayMs:0).
- Task 6: use `node:path` `posix` variant for repo-relative resolution so separators stay '/'
  regardless of OS. import-rules `resolve` only handles relative specifiers (leading '.');
  bare/external specifiers (express, os) return [] so no spurious nodes. Shared fixture lives
  at src/__fixtures__/sample-pr.ts (exports sampleRawPr + sampleRepoFiles). `repoFiles` is an
  injected Set (the real provider = `git ls-files`, lives in orchestration, not the core).
- Task 7: addIncomingEdges needs to read candidate file content to confirm imports, so it
  takes a 5th injected dep `readContent` (path)=>string|undefined in addition to `gitGrep`
  (plan listed 4 args; added readContent — confirmation needs content). Real providers in
  src/repo-scan.ts: createGitGrep/createReadContent/listRepoFiles. `git grep` exits non-zero
  on zero matches, so execFileSync throws -> catch returns []. Grep patterns per PR file =
  path-stem, basename, dotted-module (fixed strings); confirmation via import-rules drops
  false positives. PR->PR candidates are skipped (already covered by outgoing edges).
- Task 9: dashboard stack works: vite 6 + @vitejs/plugin-react + @tailwindcss/vite (Tailwind v4,
  `@import 'tailwindcss';` in index.css), @xyflow/react ^12 + @dagrejs/dagre ^1.1.4. React Flow
  custom node typing: FileNodeData must `extends Record<string, unknown>`; node type =
  `Node<FileNodeData,'file'>`, component props = `NodeProps<FileFlowNode>`. Import xyflow CSS in
  main.tsx (`@xyflow/react/dist/style.css`). dagre layout: rankdir 'LR'. Build = `tsc --noEmit &&
  vite build`. Dashboard dev server :5599 proxies /api -> :5598 (the Node server port). App loads
  the checked-in fixture for now; swap to GET /api/graph in the server/review tasks.
- Task 10: added `patch?: string` to GraphNode (src/types.ts + dashboard mirror); buildNodes now
  copies file.patch. Diff rendering uses prism-react-renderer `<Highlight language="diff">` plus a
  per-line background by first char (+ green, - red, @@ blue) so coloring is robust regardless of
  Prism's diff grammar. Selection state via zustand (src/store.ts, useSelection). Detail panel is a
  right-side absolute overlay so the graph doesn't reflow. Vite warns chunks >500kB (advisory only,
  build still exits 0).
- Task 11: server.ts default port 5598 (dashboard dev proxy points here). Entry runs only when
  process.argv[2] (data dir) is set, so importing it in tests is safe. Set PRMAP_NO_OPEN=1 to skip
  opening a browser (use in automated checks). dashboard dist resolved via
  `fileURLToPath(new URL('../dashboard/dist', import.meta.url))`. express ^4 needed for the
  `app.get('*')` SPA fallback (express 5 changed wildcard syntax). To verify a background server
  without a foreground sleep: start with `&`, then `curl --retry N --retry-connrefused --retry-delay 1`.
  createApp(dataDir) is exported so the review router (task 12) can mount on the same app.
- Task 12: createApp now takes ServerDeps {dataDir, ghClient, store} (signature changed from
  task 11). startServer reads graph.json meta.number for the store's prNumber. Review logic is
  testable operations (addComment/buildSubmission/submitReview) + a thin Router; tests use an
  in-memory ReviewStore + fake ghClient (no fs/http). buildSubmission routes line!==undefined to
  the inline comments array and line===undefined to the review body. submit clears pending ONLY on
  ok. Replies (POST /reply) call gh immediately, not batched. Verified end-to-end with curl that a
  posted comment persists to pending-review.json.
- Task 13: App now fetches GET /api/graph at runtime and falls back to the checked-in fixture if
  that fails (so the static build still renders standalone). Layout is flex: GraphView (flex-1) +
  always-visible Sidebar (w-96). Sidebar owns pending state + refresh and passes them to DetailPanel
  (per-node comment composer) and ReviewControls (pending list/delete, reply form, submit verdicts
  with a two-click confirm). DiffView lines are clickable: new-file line numbers are computed by
  walking @@ hunks; clicking sets selectedLine in the zustand store -> the composer posts an inline
  comment (else general). Submit is gated behind an explicit confirm click. Dashboard mirrors
  ReviewState/PendingComment/ReviewEvent in dashboard/src/types.ts.
- Task 14: orchestration split — deterministic half is `src/cli.ts` (npm script `map`): takes
  (ref, repoRoot), chdir to repoRoot (so gh `{owner}/{repo}` + git grep target it), fetch + assemble
  + writeGraph, prints `{dataDir, graphPath, ...}` JSON. The agent enrichment + posting are driven by
  the SKILL.md prompt, not code. Plugin is `pr-map-plugin/` with `.claude-plugin/plugin.json` +
  `skills/pr-map/SKILL.md` (reference repo uses skills/<name>/SKILL.md as the /command, no commands/
  dir). SKILL.md uses $PR_MAP_HOME for the tool checkout (packaging via CLAUDE_PLUGIN_ROOT is a TODO).
  VALIDATED read-only end-to-end against sindresorhus/slugify#75: graph = index.js (PR) + test.js
  (neighbor) with an incoming edge test.js->index.js (git-grep blast radius worked on real code);
  server served it. Remaining for a TRUE full run (task 15): agent fills summaries/why, and the user
  submits a review (outward-facing GitHub write) from the dashboard.
- Task 15: full verification green (tsc, eslint, vitest 27 tests, dashboard build). eslint needed
  argsIgnorePattern '^_' for intentionally-unused fake params. BUG found during e2e and fixed: the
  JS/TS import resolver did not map NodeNext '.js' specifiers to '.ts' source files, so it found zero
  edges in our own TS codebase. Fix: strip a trailing js/ts extension to a stem, then try all
  extensions/index (src/import-rules.ts) — added src/import-rules.test.ts. FULL e2e validated on a
  throwaway PR (ValentinDutra/pr-map#1, now closed): graph showed src/result.ts + its 10 importers as
  incoming edges (blast radius), agent enrichment filled summary/why, an inline comment + a COMMENT
  review posted to GitHub (verified via gh api), then PR/branch/.pr-map cleaned up. NOTE: GitHub
  forbids APPROVE on your own PR, so self-tests must use the COMMENT verdict.
- Bug-fix round (3 parallel review agents + empirical checks). Confirmed NON-bugs: gh `--paginate`
  merges JSON arrays (listChangedFiles safe); retry has no off-by-one; prism diff line alignment ok.
  FIXED real bugs:
  * gh-client.getFileContent now percent-encodes path segments + ref (spaces/special chars 404'd and
    silently dropped content -> missing edges). parseRepoFromUrl is host-agnostic (GHE). stdin gets an
    'error' listener so EPIPE on early gh exit can't crash.
  * import-rules.resolve strips bundler query/hash suffixes ('./x.js?worker').
  * build-graph.addIncomingEdges takes the PR file objects (not just paths) and, for renamed files,
    also searches/confirms against previousPath using an augmented file set (old path isn't on disk).
  * review-endpoints: ReviewStore now has update()/clear() (no save) with serialized mutations
    (promise chain) + atomic writes (tmp+rename) to kill the lost-update race and corruption. submit
    refuses empty COMMENT/REQUEST_CHANGES reviews and prNumber<=0 before calling GitHub (avoids 422/404).
    Async route handlers wrapped (Express 4 doesn't catch async throws -> requests used to hang). Inline
    comment requires a path; reply requires an integer commentId.
  * server: unmatched /api/* now returns JSON 404 instead of the SPA index.html (HTML 200).
  * dashboard graph-view: nodes are draggable now (useNodesState/useEdgesState + onNodesChange) — without
    it React Flow v12 warns and won't move nodes. App fetch uses AbortController (StrictMode double-fetch).
    confidence shown as a percentage.
  * dashboard diff-view: dropped prism-react-renderer entirely — its bundled Prism has NO 'diff' grammar,
    so highlighting was dead; render plain lines with +/- backgrounds + clickable gutter. Uninstalled the
    dep (also shrinks the bundle).
  Re-validated read-only e2e (slugify#75) after fixes: no regressions; /api/nope -> 404 JSON. 32 tests.

V2 EXECUTION NOTES
- Task 2: merge-enrichment.ts is both an importable module (mergeEnrichment pure fn, tested) and a
  CLI (dist/merge.js). Guard "run main only when invoked directly" with
  `import.meta.url === pathToFileURL(realpathSync(process.argv[1])).href` so importing it in vitest
  does NOT run main() (which would process.exit and kill the test run). Enrichment files are tolerant:
  accept a single EnrichmentResult, an array, or {files:[...]}, and skip unparseable JSON.
- Task 4: esbuild bundles src/{cli,server,merge-enrichment}.ts -> pr-map-plugin/dist/{cli,server,merge}.js
  (format esm, platform node, target node20, bundle true). REQUIRED banner so CJS deps (express/open)
  that call require at runtime work in ESM output:
  `import { createRequire as __createRequire } from 'module'; const require = __createRequire(import.meta.url);`
  express + open bundle fine (no externals needed). All three run with plain `node` (no tsx). .gitignore
  changed `dist/` -> `/dist/` so pr-map-plugin/dist is committed (the dev root has no dist/). Committed
  artifacts go stale on source/dashboard changes -> final task re-runs `npm run build:plugin`.
- Task 8: Claude Code plugin hooks live in `pr-map-plugin/hooks/hooks.json` and are AUTO-DISCOVERED
  (plugin.json does NOT reference them — confirmed vs the langsmith-tracing plugin). Format:
  `{ "hooks": { "SessionEnd": [ { "hooks": [ { "type":"command", "command":"bash \"${CLAUDE_PLUGIN_ROOT}/hooks/cleanup-server.sh\"", "timeout":10 } ] } ] } }`.
  cleanup-server.sh kills the PID at ${TMPDIR:-/tmp}/pr-map-server.pid (matches node os.tmpdir()).
  Verified it kills a live server. Keep the script's +x bit committed.
- Task 10: committing the plugin bundle made eslint lint the minified artifacts -> add
  `pr-map-plugin/**` to eslint ignores. Plain `.mjs` build scripts need node globals declared in
  eslint flat config (`files:['**/*.mjs'], languageOptions.globals:{console,process,URL}`) because
  no-undef is on for JS (off for TS). After ANY dashboard/src change, re-run `npm run build:plugin`
  to refresh committed `pr-map-plugin/dashboard-dist`. Full v2 verified: tsc/eslint 0, 34 tests,
  dashboard build + build:plugin 0, and bundled e2e (slugify#75 + hand-written enrichment) shows a
  node carrying insights via /api/graph.

V3 EXECUTION NOTES
- Task 1: changed-symbols.ts extractChangedSymbols({patch,headContent?,language,status?}) -> Set<string>.
  Language strings come from build-graph detectLanguage: "typescript"/"javascript"/"python"/"json"...
  Hunk header context (after 2nd @@) names the enclosing decl (git function-context) -> main signal for
  modified files; added files parse all exports from headContent (patch alone also works since all +lines).
  Only EXPORTED names matter (internal types like NewDebt are not importable, so not needed for matching).
