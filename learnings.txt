Round 1 (#58) landed the orphan-safety MVP: server-pids.ts (PID_DIR + best-effort write/removePidFile) + the spawner writes ${port}-llama.pid right after spawn and removes it on stop()+exit. /review then found 2 issues for round 2: (1) CORRECTNESS -- removal is port-keyed and content-blind, so on a model swap where the OS reuses the just-freed port, the OLD child's late exit handler deletes the NEW child's pid file and re-orphans it; fix by making removePidFile content-aware (rm only if the file still holds this child's pid) and gating write+both removals on a captured spawnedPid. (2) TEST-INTEGRITY -- the write-on-cold-start invariant was untested (the "health never returns ok" test injected a no-op writePid), so deferring the write into the health-ok branch would pass; make that test record writePid and assert 7777-llama.pid + pid 12345. Keep the write RIGHT AFTER spawn, best-effort, never inside pollHealth (mutex-wedge risk).
