Round 1 (#59): the 6-task robot+popup MVP landed. Parallel review and /review both confirmed findings #1 (multi-turn code-context loss) and #2 (scroll-follow dead on small diffs) are RESOLVED by commits a5db47b/f5a9590 -- do NOT reopen or re-touch them. Two regressions remain, now the round-2 tasks: (A) the gutter cell was widened w-6->w-8, shifting every diff row 8px right at rest -- restore w-6 and float the robot as an absolute overlay (no reserved flow width). (B) a write-once contextMessageRef makes a retry after a failed first send re-send the stale first question -- build outgoing messages from the live question each send via a pure buildOutgoingMessages helper, and DELETE contextMessageRef entirely (the pure test locks the builder; the ref deletion + live-question wiring is the actual fix). Keep the first-message format byte-identical.
